language: python
python:
  - 2.6
  - 2.7
  - "pypy"
install:
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip-2.6 install sympy; else pip-2.7 install sympy; fi
  - "python setup.py install"
script:
  - "python mathics/test.py"
notifications:
  email: false
